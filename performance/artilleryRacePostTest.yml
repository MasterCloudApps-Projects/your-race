config:
  target: "http://192.168.49.2:32002"
  #"http://192.168.49.2:31660"
  #192.168.49.2:31097
#  payload:
#      # path relativo a la localización del script de test
#      path: "users.csv"
#      fields:
#        - "username"
#        - "password"
  phases:
    - duration: 10
      arrivalRate: 6

  payload:
    path: "aplicationId.csv"
    fields:
        - "applicationCode"

scenarios:
  - name: "Search and buy"
    flow:
      - post:
          url: "/api/tracks/byorder"
          json:
            applicationCode: "{{ applicationCode }}"
          capture:
            - json: "$.id"
              as: "trackId"
